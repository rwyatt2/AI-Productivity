{
    "private": true,
    "version": "1.4.0",
    "scripts": {
        "sync": "node scripts/sync-kit-snippets.mjs",
        "sync:starter": "node scripts/sync-starter-from-kit.mjs",
        "site:start": "npm --prefix site run start",
        "site:build": "npm --prefix site run build",
    "doctor": "node scripts/doctor.mjs",
    "hooks:install": "node scripts/install-githooks.mjs"
    }
}
